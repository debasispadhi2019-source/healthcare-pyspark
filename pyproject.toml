[project]
name = "healthcare-pyspark"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "faker>=40.38.0",
    "pandas>=3.0.5",
    "pyarrow>=25.0.1",
    "pyspark>=4.2.0",
]

[project.scripts]
healthcare-pyspark = "healthcare_pyspark:main"

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"
